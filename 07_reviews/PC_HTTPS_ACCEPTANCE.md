# 电脑后端固定HTTPS接入记录

日期：2026-09-08。依据MADR-041四项方案。状态：NOT_READY，连接阶段未通过。

## 已确认

- 用户已注册ngrok，浏览器控制台可以访问；跳过可选新用户问卷。
- Domains页面确认账号分配的Dev Domain为 `outreach-alibi-reformer.ngrok-free.dev`。这证明地址已分配，不能证明隧道已在线。
- 本机后端 `127.0.0.1:4318` 健康响应正常，运行于独立pc-server目录；预算仍默认关闭，未产生真实模型调用。
- 原APK仍是loopback调试包，未声称新域名APK交付成功。

## 凭据边界

为避免把Authtoken输出到聊天，尝试通过浏览器工具将已显示凭据的整页导出到本机。自动审批拒绝该动作，理由是整页持久化敏感内容超出本机隧道配置所需，并存在更小范围替代方案。没有成功导出整页或把token写入项目配置。

后续仅允许按明确授权将单个Authtoken保存到受保护的本机连接配置，或由用户自行本地填写；禁止导出整页、读取浏览器存储/cookie、绕过拒绝或把凭据写入APK/仓库。不要求用户在聊天里发送密钥。

## Windows客户端阻碍

下载地址从官方 `https://ngrok.com/download/windows` 页面提取：`https://bin.ngrok.com/c/bNyj1mQVY4c/ngrok-v3-stable-windows-amd64.zip`。ZIP下载与CRC检查完成，解压的ngrok.exe在读取哈希/版本之前消失。

只读检查Windows Defender事件1116/1117，确认该文件被识别为 `Trojan:Win32/Kepavll!rfn`，严重等级，已成功隔离。此处登记检测结果，不自行断言真实恶意或误报。版本和发布者签名尚未取得验证结果，程序未成功运行、未启动隧道。

没有恢复隔离文件、关闭防护、添加排除项、改名重试或换SDK规避检测。未把样本上传第三方。需先由可信渠道核实检测并取得可通过安全验证的官方客户端，再继续连接阶段；不为完成验收放宽系统安全。

## 继续条件

1. 客户端来源、完整性与安全阻碍解决。
2. 最小范围的单项Authtoken本机配置完成，避免记录请求正文的流量检查功能。
3. 仅将固定HTTPS代理到4318，核验健康接口、TLS信任、未登录401与管理路径403。
4. 重启地址保持不变，小米手机流量实际可达。
5. 核价与预算后完成合成模型调用，再以真实地址签出APK并真机验收。

账号注册、客户端安装、公网连通和APK交付是不同状态，不合并标记完成。四项决策不变，不自动切换其他托管路线或采购资源。

## 续作验证

2026-09-08网络检索补充：按MADR-042从ngrok FAQ进入官方版本归档，核对HTML中SHA256字段，确认本机ZIP哈希与3.39.11 Windows amd64 ZIP完全一致。这补足了发布包完整性证据，不能替代EXE签名或安全检测结论。微软官方建议识别检测来源、提交疑似误报分析；提交入口本轮HTTP403，未发送资料。下一步为正常渠道安全情报更新/厂商核实，再验证客户端；本轮未更新防护、未恢复隔离。详细来源与适用限制见[MADR-042](../03_decisions/MADR-042.md)。

- 官方FAQ（https://ngrok.com/docs/faq ，2026-09-08读取）说明ngrok有时被杀毒软件标记，建议检查官方来源与向安全厂商报告。它不是针对当前ZIP或Kepavll检测的核验结论；本次阻碍未解决。
- 新增 `native/scripts/https_acceptance.py`：仅GET健康、未登录库、根路径及管理API；限制JSON体积、不跟随重定向、不提交凭据或模型调用，使用默认TLS证书验证。电脑探测不能替代小米流量验收。
- 本机实测200/401/403/403全部通过；固定域名本轮四项均URLError，未取得有效API响应，不把它单独归因为服务端故障（可能包含当前网络/DNS问题）。
- 5项合成测试通过：正常API、HTML提示页、过大/错误版本响应、重定向及URL约束。PC生命周期回归1项通过。
- `build_release.py` 在打开签名密码前先运行HTTPS验收；实际调用确认当前地址失败并停止，无新APK。底层构建工具仍可供CI使用，该检查不是完整发布审批。
- 修正PC服务提示与部署README的旧Tailscale交付说明。UI未改。
