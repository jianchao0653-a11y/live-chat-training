# v0.17.3 多版本兼容与本机交付记录

本轮源码已推送私有分支 `codex/mobile-acceptance-v014`：`710285b44f15e36a8a8cc8cbb375bb0504a6b8a4`（27个白名单文件），Draft PR #3 未合并。按该 SHA 查询 Actions 为0条运行，手动CI策略未消耗新托管作业。未上传原始截图、运行数据库、密钥或邀请文件。项目模拟器已关闭（adb devices为空），电脑后端PID25392继续运行、loopback可达；后续以控制器status为准。

## 范围与当前证据

兼容目标是多机型、多 Android 版本；小米仅是第一台待验真机。沿用 UI、九宫格/全键盘切换与记忆、主播—客户隔离、人工审批插入发送。当前不是全部机型验收通过。

- GitHub 工作分支手动 CI 策略提交：`a8126b1c2252e152f0877a832ceab746271ece1a`，Draft PR #3 未合并。账户截图显示免费分钟耗尽，Billable usage $0；不购买额度。
- ngrok Full Capture：用户确认已关闭，未独立检查设置页面。
- 本机第一次完整 prepare：`output/local-delivery/20260909-164916-1ad67b0d/report.json`，Node 71、策略、HTTPS、20 合成评估、API36 云端 UI/OCR 校对及双 ABI 构建通过。之后扩展兼容脚本和基准，旧回执不可用于当前源文件签名。
- 初次单尺寸 OCR 实验：1080×2400，开发集20、验证集20。验证集旧策略13/20逐字一致、新策略18/20；字符编辑距离9/160→2/160；中位时延2105.5→2516.5毫秒。开发集存在3项退步。不是实际聊天截图、说话人归属质量验收。
- API36 四尺寸新基准已完成160次离线对照：验证集每尺寸10条，旧→新依次为720×1600 10→10、1080×2400 8→7、1440×3200 9→9、2400×1080 8→9。总35/40→35/40，字符错误6/320→5/320；保留高像素的两个尺寸中位时延约增加0.4秒。鉴于存在分尺寸退步，产品默认保留原有采样；高像素策略仅保留为实验。原始证据 output/plan-20260909/ocr-matrix-api36.json。
- v0.17.3 正式签名测试包已生成：`output/native/conversation-lens-0.17.3-cloud-release.apk`，code21，SHA256 `0af021631a5f4cefa9c22d2fb73eda33a6d57e7f0150a5976971e7d1ce538d80`。原证书 SHA256 `0ce1d2c5fd366f7b441296fa483a2af20be18f5b5dd1f10c791f6b4eb4cd692e` 不变，签名与16KB APK对齐检查通过；项目模拟器正式包0.17.2→0.17.3覆盖安装/启动通过（不是已有真人账号资料迁移验收）。
- API34 最终回执 `output/compatibility/api34-20260909-171700/report.json` 全部 PASS，APK 与 API26 同哈希。包括三种显示配置的校对契约、登录/主播档案/人物记忆、建议编辑确认插入、反馈落库、重启后修改删除；旧 CI 中 API34 重启后记忆按钮定位问题本次合成复验通过。API36 初轮同范围通过于 `output/compatibility/api36-20260909-170446/report.json`，最终稳定采样代码另经交付 prepare 复验。
- 选图默认已恢复原有1600边长采样（另加非法尺寸拒绝）；主动屏幕捕获 `CaptureService` 仍采用现有1600边长缩放。后续需用多行真实聊天合成布局单独验证捕获与说话人归属。

最终回执：`output/local-delivery/20260909-172107-21ad97e1/report.json` 状态 SIGNED_TEST_BUILD。API36 最终业务与校对、Node71、输入/图像策略、HTTPS5、20本地合成评估、双ABI构建、原身份签名与源码部署ZIP均通过；公网 health200、未认证library401、管理接口403符合预期。原签名包覆盖启动证据 `output/plan-20260909/release-upgrade.json`。一键入口已完成底层分阶段实跑；不把跨Windows身份自动解锁或任意电脑双击视为已验收。

可交付为测试包，仍保持 qualityAccepted=false、productionReady=false。API26/34使用同一双ABI debug APK 完成矩阵；最终API36 prepare使用 x86_64 debug 后构建双ABI release。真机及真实多行OCR、全机型声明、人工质量尚未放行。

## 兼容矩阵与放行标准

| 维度 | 覆盖范围 | 验收标准 |
| --- | --- | --- |
| Android 版本 | 首批 API26/34/36；后补28/30/31/33/35 | 安装启动、登录、资料隔离、九宫格选词、建议确认插入、OCR 校对、取消及失效场景无崩溃 |
| 厂商系统 | 小米 HyperOS；OPPO/一加 ColorOS；vivo OriginOS；三星 One UI；按实际用户补荣耀 | 至少覆盖多个厂商实机；记录精确型号/系统/安全补丁；权限引导可完成，切换 App 后恢复正常 |
| 显示 | 窄屏、长屏、横屏；标准及1.3倍字体，后补最大字体/折叠窗口 | 按钮可见或可滚动到达，输入区与候选词不遮挡；旋转不会自动批准片段 |
| 聊天 App | 微信、QQ，按实际场景追加 | 正常文本输入、九宫格/全键盘切换记忆、编辑后插入；由本人发送 |
| OCR | 720×1600、1080×2400、1440×3200、2400×1080；28/32/36/40像素文字、深浅色、左右位置 | 分尺寸报告逐字匹配/字符错误/时延和退步；另需真实聊天多行、数字、否定句及说话人校验；无结果可转手工粘贴 |
| 网络与生命周期 | Wi-Fi、流量、断网重连、后台返回 | 离线可打字和本地 OCR；联网建议失败能重试，跨账号/客户不混用，不自动发送 |
| 安装架构 | 当前 arm64-v8a/x86_64，minSdk26 | 同签名覆盖升级，资料不丢失；32位手机当前不支持，不夸大兼容范围 |

一次模拟器修改分辨率不是新增一台真机；同一 API 三种显示配置也不是三个 Android 版本。自动化使用程序操作的 OCR 校对测试仅验证逻辑契约，真机仍需手动触摸检查布局。

## 真机与人工试用记录模板

API26 首轮合成流程未通过：填写邀请码后，登录同意框在键盘上方视口以外，原 QA 从 height-220 开始滑动，触及键盘而非 ScrollView。原失败回执保留 `output/compatibility/api26-20260909-170749`。修复 QA 使用可见 ScrollView bounds 计算滑动起止，不添加品牌或 API26 特判；复验结果后续回填。

API26 第二轮通过登录/资料后，建议详情断言读取早于正文出现；随后独立层级检查显示原文、策略、风险均完整存在。采用已有异步页面等待方式，不改变产品。最终 `output/compatibility/api26-20260909-171134/report.json` 全部 PASS：三种显示配置的校对契约与标准配置完整合成业务流程。API26 当前 APK SHA256 `cd91df3651667abf3ec8054768a849f0a162a6c85d561f90f35c80e5a651d00f`。

API26 `svc wifi disable` 返回137，未确认原因，不反复尝试或把它归因为产品崩溃；本轮不标该系统断网 OCR 已通过，独立引擎实验另记。API36 四尺寸对照在成功关闭模拟器网络后执行。

API26 引擎实验已完成30张合成图，旧的宽松匹配（去空格后 contains）24/30，exact_text=FAIL；此分数不能与四尺寸严格逐字匹配直接比较。回执 `output/plan-20260909/ocr-engine-api26.txt` 首行已明确网络隔离未建立；runner 的 offline_capability 字段命名不能作为该轮离线证据。

已读官方资料（2026-09-09）：<https://developer.android.com/develop/ui/views/touch-and-input/keyboard-input/visibility>，adjustResize 下控件应在可用视口中滚动访问。实际层级显示键盘上缘约711，ScrollView内容裁剪在703，原固定滑动起点1060。此证据支持先修正验收手势；不能据此声称所有实际布局问题都只是测试错误。

API36 最终 prepare 首轮 `output/local-delivery/20260909-171837-450d36d4` 未通过：读取建议详情时当前前台已是 Launcher，crash buffer 无崩溃条目。API26 的视口修正仅用 bottom-50，边到边窗口可能包含导航区域；API36 实际导航栏位于1184—1280。已阅读 <https://developer.android.com/develop/ui/views/layout/edge-to-edge>（2026-09-09），确认窗口可延伸到系统栏后方。QA 改为视口内部25%→75%的手势并限制目标点击不靠近屏幕上下系统区域；未给特定品牌/API单独打补丁。最终复验回执另记，不覆盖失败记录。

每条填写：APK版本/哈希、手机型号、Android与厂商系统版本、字体大小、聊天App版本、网络类型、操作步骤、期望/实际结果、是否可复现。素材先脱敏，人物/聊天截图不进入源码仓库。

按顺序验证：安装登录 → 启用输入法 → 九宫格选词/切换记忆 → 选主播与客户 → 主动导入可见聊天并逐行校对 → 获得建议、修改、确认插入 → 本人发送 → 更换客户/账号验证隔离 → 断网及后台恢复。屏幕保护导致的黑屏不作为崩溃，不绕过保护。

人工质量与小规模试用（3人×7天）仍待实际执行，不通过合成测试代签。预算维持全局20次分析/日、1元/日、10元/月；本轮测试不调用付费模型。
